# =========================================================
# ChartSpeak -- unified project requirements
# One shared environment for the whole team, so Person 1's
# extraction pipeline and Person 2's analysis engine (this repo)
# can run side by side without two conflicting installs.
# =========================================================

# ----- Person 1 (Chart4Blind / chart extraction) -----
opencv-python>=4.8.0
numpy==1.26.4
pydantic==2.6.4
matplotlib==3.8.3
scikit-learn==1.4.1.post1
pillow>=10.4.0
openai==1.14.1
pytest==8.1.1
python-dotenv==1.0.1
shapely
pyclipper
lmdb
tqdm
visualdl
rapidfuzz
cython

# ----- Person 2 (ChartSpeak analysis/salience/narration/API) -----
# numpy and pydantic above are PERSON 1'S exact pins, not the versions
# this engine was originally built and tested against (numpy 2.4.6,
# pydantic 2.13.5). We re-ran every stage (1-11) of this engine under
# exactly these downgraded versions and got byte-for-byte identical
# output -- see the write-up for how this was verified. If a future
# stage ever needs a numpy/pydantic feature that genuinely requires a
# newer version, that's a conversation to have with Person 1 before
# just bumping the pin here, since it's now a shared number.
scipy==1.17.1

# IMPORTANT: fastapi must stay below 0.130 while pydantic is pinned to
# 2.6.4 -- fastapi 0.130+ requires pydantic>=2.7.0, and 0.138+ requires
# pydantic>=2.9.0, which would conflict with Person 1's pin above. This
# exact version was verified to actually run our API correctly (not
# just "resolves" -- we started the server and made real requests).
fastapi==0.125.0
uvicorn==0.52.4

# Required for the /analyze-file endpoint (Stage 13, frontend
# integration) -- FastAPI's UploadFile/File() multipart form parsing
# depends on this package being installed; it raises at import time
# otherwise.
python-multipart==0.0.32

# Required for image_extractor.py (Stage 14, real JPG/PNG -> ChartCSR
# digitization) -- this is a thin Python wrapper around the Tesseract
# OCR engine, used to read axis tick labels and titles off the chart
# image itself.
#
# IMPORTANT: pytesseract does NOT bundle the OCR engine. You must also
# install the "tesseract-ocr" SYSTEM binary separately (this is not a
# pip package):
#   Debian/Ubuntu:  sudo apt-get install tesseract-ocr
#   macOS:          brew install tesseract
#   Windows:        https://github.com/UB-Mannheim/tesseract/wiki
# If pytesseract can't find the binary, either put it on PATH or set
# `pytesseract.pytesseract.tesseract_cmd` to its install location.
pytesseract==0.3.13

# Optional -- only needed if you set ANTHROPIC_API_KEY to enable the
# Stage 10 LLM narration path. Everything works without it (falls back
# to template narration automatically). Uncomment to install:
# anthropic
